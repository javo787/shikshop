import CartClient from './CartClient';

export const metadata = {
  title: 'Корзина | PARIZOD',
  description: 'Оформление заказа в магазине PARIZOD',
};

export default function CartPage() {
  return <CartClient />;
}